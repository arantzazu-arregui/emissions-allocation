# Notebooks

Use numbered notebooks for narrative or visual analysis, for example `01_exploratory_presence.ipynb`. Read inputs from `data/`, write figures to `reports/figures/`, and keep reusable logic in `src/` or `scripts/`.
