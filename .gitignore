# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Secrets — never commit API tokens
.env
*.token

# Data (raw, interim, processed, and generated outputs are not versioned).
# data/sample/api/ IS tracked: it holds the captured raw API responses that the
# behaviour documented in CLAUDE.md is sourced from. See scripts/exploratory/.
data/*
!data/sample/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Agent tooling (local only)
.claude/
