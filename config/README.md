# Configuration

Active analysis settings (regions, date range, vessel classes, and track settings) currently live at the top of `scripts/fetch_presence.py`. Keep future non-secret, versioned configuration files here.

Secrets belong in the local `.env` file or environment variables and must never be committed.
