requests>=2.31
cartopy
pandas>=2.0
pyarrow>=15.0
duckdb>=1.0
pyyaml>=6.0
openpyxl>=3.1
matplotlib>=3.8
pytest>=8.0

# Notebook (docs/METHODOLOGY.md walkthrough)
jupyter>=1.0
nbformat>=5.9

# NOT required: geopandas / shapely / pyogrio.
# Spatial work runs in DuckDB's `spatial` extension, which reads GeoPackage through
# GDAL -- including from inside a zip via /vsizip/ -- so point-in-polygon and
# distance operations stay in src/emissions_allocation/sql/*.sql where the
# architecture decision puts them. The extension is installed by DuckDB itself on
# first use (see db.Database.ensure_spatial), not by pip.
